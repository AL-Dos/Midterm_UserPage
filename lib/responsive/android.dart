import 'package:flutter/material.dart';

class AndroidBody extends StatelessWidget {
  const AndroidBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('M O B I L E'),
        centerTitle: true,
      ),
    );
  }
}
